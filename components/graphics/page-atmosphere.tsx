/* Decorative page lighting is intentionally omitted: the canvas is a solid
   warm charcoal so content and the infinity mark provide the only depth. */

export function PageAtmosphere() {
  return null;
}
